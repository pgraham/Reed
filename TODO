- Add logging interface:

  -What is already out there?
  -log4j has good extensible structure, can it be ported?

- Move tag addition into CodeBlock from CodeTemplate
- Move substitution into a helper class used by CodeTemplate, IfBlock and
  EachBlock to perform template resolution in a uniform way.  This will let
  Array indexed substitution and detection to be handled in one spot
  respectively.
- Update EachBlock and IfExpression evaluation to handle indexed references.
